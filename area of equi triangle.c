#include <stdio.h>
#include <math.h>
int main()

{
    int a;
    printf("enter side side of a triangle:");
    scanf("%d",&a);
    printf("area of equilateral triangle is:%g",sqrt(3)/4*a*a);
    return 0;
}
 
